/**
 * Placeholder — substitua este arquivo pelo tipo gerado de verdade assim que o projeto
 * Supabase estiver criado e as migrations aplicadas:
 *
 *   npx supabase login
 *   npx supabase link --project-ref <seu-project-ref>
 *   npx supabase gen types typescript --linked > src/lib/supabase/database.types.ts
 *
 * Até lá, mantemos `Database` propositalmente permissivo para não fingir uma tipagem
 * que ainda não foi conferida contra o schema real.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
